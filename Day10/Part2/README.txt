while loop to count how many items in the list.

then find the mid of the linked list.

while loop again, when the list is looped in the mid, append the value to an empty list.

Finally, reverse that empty list, and append the value to the ListNode.